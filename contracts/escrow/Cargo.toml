[package]
name = "escrow"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]
doctest = false

[dependencies]
soroban-sdk = { workspace = true }
lending-pool = { path = "../lending-pool" }

[dev-dependencies]
soroban-sdk = { workspace = true, features = ["testutils"] }
